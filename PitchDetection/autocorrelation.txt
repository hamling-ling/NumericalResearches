信号 x = {...} があったら
パワースペクトルは元信号をフーリエ変換したもの。
P=|F(x)^2|

自己相関関数のフーリエ変換でもパワースペクトルが求まる。
P=F(r)/N

自己相関をは直接求めると計算コストがかかるので、x を fft して P を
求め、それをフーリエ逆変換して求めるのが一般的。
r=N*F'(P)

x_fft = fft(x);
P = x_fft*conj(x_fft);
r=N*ifft(P);
